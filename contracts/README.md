# Contracts

This directory holds JSON schemas for agent-readable project metadata.

- `task.schema.json` validates entries in `tasks.json`
- `decision.schema.json` validates entries in `decisions.json`
- `pattern.schema.json` validates entries in `patterns.json`
